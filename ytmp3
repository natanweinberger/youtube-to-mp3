function main() {
    [[ -z "$SPOTIFY_DIR" ]] && echo "\$SPOTIFY_DIR is not set in your environment." && return;
    docker run --rm -it -v $SPOTIFY_DIR:/home youtube-to-mp3 bash main.sh $@
}

main $@
